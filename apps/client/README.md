# Client

Vite + Vue 3 + TypeScript skeleton.

## Usage

You need to start the **backend** service beforehand to make the project work properly.

- dev: `npm run dev`
- build: `npm run build`
- preview: `npm run preview`
